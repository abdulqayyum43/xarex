fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
# grpcio + protobuf must be aligned with the proto stubs in cloud-brain/proto/
# which were generated against protoc-gen-python 6.31.x. Protobuf python jumped
# directly from 5.29 → 6.30 (skipping 5.30/5.31), so the matching runtime is
# the 6.x series, NOT 5.31 as the gencode version number suggests.
# This silences the "gencode 6.31.1 runtime 5.27.0" startup crash.
grpcio>=1.68.0
grpcio-tools>=1.68.0
protobuf>=6.31.1,<7.0
websockets==12.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
httpx==0.27.0
pydantic==2.7.1
pydantic-settings==2.2.1
structlog==24.2.0
networkx==3.3

# ── New: AI & Autonomous features ──────────────────────────────
anthropic>=0.28.0
apscheduler>=3.10.4
croniter>=2.0.1

# ── New: CVE Enrichment ─────────────────────────────────────────
# httpx already included above

# ── New: Notifications ──────────────────────────────────────────
# httpx already included above

# ── New: Report generation ──────────────────────────────────────
# built-in (string templates, no Jinja2 needed)

# ── Billing ─────────────────────────────────────────────────────
stripe>=9.0.0
resend>=2.0.0
python-multipart>=0.0.9
jinja2>=3.1.4

# ── Rate limiting ────────────────────────────────────────────────
# slowapi: per-route declarative rate limits keyed by client IP. In-memory
# by default (per-process); can be switched to a Redis backend in production
# by setting a storage_uri on the Limiter instance in `limiter.py`.
slowapi>=0.1.9
