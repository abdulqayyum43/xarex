[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    # SQLAlchemy 2.x deprecation noise from third-party libs
    ignore::DeprecationWarning:passlib.*
addopts = -ra --strict-markers
markers =
    slow: marks a test as slow (deselect with -m 'not slow')
